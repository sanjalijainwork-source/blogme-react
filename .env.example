# SQLite-compatible database (Turso). Optional for local dev.
# If unset, the app uses a local file (blog.db) or falls back to browser storage.
# Get these from https://turso.tech or from Vercel after adding the Turso integration.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
